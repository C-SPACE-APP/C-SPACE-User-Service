version: '3'
services:
  app:
    container_name: user-service
    build: .
    ports:
      - '3002:3002'
    depends_on:
      - user-db
    networks:
      - cspace-backend
      - user-network
        
  user-db:
    container_name: user-db
    image: mongo
    ports:
      - '28002:27017'
    networks:
      - user-network

networks:
   cspace-backend:
      external: true
   user-network:
      external: true
